#docker stack necessite  docker swarm (cluster)
#docker  stack  deploy -c  docker-compose.yml   my-stack
#utilisation ici de docker-compose utilisant systématiquement docker-compose.yml

docker-compose up &
